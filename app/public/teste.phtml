<!-- Contêiner para os cards -->
<div class="container">
        <div class="container-cards">
            <div class="row card-container">
                <?php foreach ($cards as $card): ?>
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="<?= htmlspecialchars($card['image_url']) ?>" class="card-img-top" alt="Imagem do Card">
                            <div class="card-body">
                                <h5 class="card-title"><?= htmlspecialchars($card['title']) ?></h5>
                                <p class="card-text"><?= htmlspecialchars($card['description']) ?></p>
                                <div class="d-flex justify-content-between">
                                    <a href="/delete?id=<?= $card['id'] ?>" class="btn btn-danger">Excluir</a>
                                    <a href="/favorite?id=<?= $card['id'] ?>" class="btn <?= $card['is_favorite'] ? 'btn-warning' : 'btn-outline-warning' ?>">
                                        <?= $card['is_favorite'] ? 'Desfavoritar' : 'Favoritar' ?>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
    </div>